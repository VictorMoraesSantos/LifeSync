<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LifeSyncApp.Controls.EmptyState"
             x:Name="EmptyStateControl">
    
    <VerticalStackLayout Spacing="16"
                         Padding="32"
                         HorizontalOptions="Center"
                         VerticalOptions="Center">
        
        <!-- Icon -->
        <Image Source="{Binding Icon, Source={x:Reference EmptyStateControl}}"
               WidthRequest="120"
               HeightRequest="120"
               HorizontalOptions="Center"
               Opacity="0.5" />
        
        <!-- Title -->
        <Label Text="{Binding Title, Source={x:Reference EmptyStateControl}}"
               Style="{StaticResource H3}"
               HorizontalOptions="Center"
               HorizontalTextAlignment="Center" />
        
        <!-- Description -->
        <Label Text="{Binding Description, Source={x:Reference EmptyStateControl}}"
               Style="{StaticResource BodyMedium}"
               TextColor="{StaticResource TextSecondary}"
               HorizontalOptions="Center"
               HorizontalTextAlignment="Center"
               MaximumWidthRequest="300" />
        
        <!-- Action Button -->
        <Border Style="{StaticResource PrimaryButton}"
                IsVisible="{Binding ShowActionButton, Source={x:Reference EmptyStateControl}}"
                Margin="0,8,0,0">
            <Border.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding ActionCommand, Source={x:Reference EmptyStateControl}}" />
            </Border.GestureRecognizers>
            
            <Label Text="{Binding ActionButtonText, Source={x:Reference EmptyStateControl}}"
                   Style="{StaticResource BodyMedium}"
                   TextColor="{StaticResource TextInverse}"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   VerticalOptions="Center" />
        </Border>
        
    </VerticalStackLayout>
    
</ContentView>
