@page "/gym"
@using LifeSyncApp.Client.Services
@using LifeSyncApp.Client.Models.Gym
@inject IGymService GymService
@using System.Security.Claims

<PageTitle>Academia - LifeSync</PageTitle>

<RedirectToLogin>
<div class="content-header">
    <div>
        <h1 class="page-title">Academia</h1>
        <p class="page-description">Gerencie seus treinos e exercícios</p>
    </div>
</div>

<div class="stats-grid">
    <div class="stat-card">
        <div class="stat-header">
            <div>
                <div class="stat-value">@totalWorkouts</div>
                <div class="stat-label">Treinos Totais</div>
            </div>
            <div class="stat-icon primary">
                <span>??????</span>
            </div>
        </div>
    </div>

    <div class="stat-card">
        <div class="stat-header">
            <div>
                <div class="stat-value">@weekWorkouts</div>
                <div class="stat-label">Treinos esta Semana</div>
            </div>
            <div class="stat-icon success">
                <span>??</span>
            </div>
        </div>
    </div>

    <div class="stat-card">
        <div class="stat-header">
            <div>
                <div class="stat-value">@routinesCount</div>
                <div class="stat-label">Rotinas Ativas</div>
            </div>
            <div class="stat-icon warning">
                <span>??</span>
            </div>
        </div>
    </div>

    <div class="stat-card">
        <div class="stat-header">
            <div>
                <div class="stat-value">@exercisesCount</div>
                <div class="stat-label">Exercícios</div>
            </div>
            <div class="stat-icon secondary">
                <span>??</span>
            </div>
        </div>
    </div>
</div>

<div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem; margin-bottom: 1.5rem;">
    <button class="btn btn-primary" style="height: 60px;" @onclick="StartWorkout">
        ?? Iniciar Treino
    </button>
    <button class="btn btn-secondary" style="height: 60px;" @onclick="ManageRoutines">
        ?? Gerenciar Rotinas
    </button>
    <button class="btn btn-secondary" style="height: 60px;" @onclick="ManageExercises">
        ?? Gerenciar Exercícios
    </button>
</div>

<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem;">
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Treinos Recentes</h3>
        </div>
        <div class="card-body">
            @if (isLoadingWorkouts)
            {
                <div style="text-align: center; padding: 2rem;">
                    <div class="loading"></div>
                    <p style="margin-top: 1rem;">Carregando...</p>
                </div>
            }
            else if (recentWorkouts.Any())
            {
                <div style="display: flex; flex-direction: column; gap: 1rem;">
                    @foreach (var workout in recentWorkouts.Take(5))
                    {
                        <div style="padding: 1rem; background: var(--color-background-tertiary); border-radius: var(--border-radius);">
                            <div style="display: flex; justify-content: space-between; margin-bottom: 0.5rem;">
                                <h4 style="margin: 0;">Rotina #@workout.RoutineId</h4>
                                <span class="badge badge-primary">@((workout.EndTime.HasValue ? (int)(workout.EndTime - workout.StartTime)?.TotalMinutes : 0)) min</span>
                            </div>
                            <p style="font-size: 0.875rem; color: var(--color-text-terciary); margin: 0;">
                                @workout.StartTime.ToString("dd/MM/yyyy HH:mm")
                            </p>
                        </div>
                    }
                </div>
            }
            else
            {
                <div style="text-align: center; padding: 2rem;">
                    <div style="font-size: 2.5rem; margin-bottom: 1rem;">??</div>
                    <p style="color: var(--color-text-terciary);">Nenhum treino registrado</p>
                </div>
            }
        </div>
    </div>

    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Minhas Rotinas</h3>
        </div>
        <div class="card-body">
            @if (isLoadingRoutines)
            {
                <div style="text-align: center; padding: 2rem;">
                    <div class="loading"></div>
                    <p style="margin-top: 1rem;">Carregando...</p>
                </div>
            }
            else if (routines.Any())
            {
                <div style="display: flex; flex-direction: column; gap: 1rem;">
                    @foreach (var routine in routines)
                    {
                        <div style="padding: 1rem; background: var(--color-background-terciary); border-radius: var(--border-radius); cursor: pointer;" 
                             @onclick="() => StartRoutine(routine)">
                            <h4 style="margin: 0 0 0.5rem 0;">@routine.Name</h4>
                            <p style="font-size: 0.875rem; color: var(--color-text-secondary); margin: 0;">
                                @routine.Description
                            </p>
                        </div>
                    }
                </div>
            }
            else
            {
                <div style="text-align: center; padding: 2rem;">
                    <div style="font-size: 2.5rem; margin-bottom: 1rem;">???</div>
                    <p style="color: var(--color-text-terciary);">Nenhuma rotina criada</p>
                    <button class="btn btn-sm btn-primary" style="margin-top: 1rem;" @onclick="ManageRoutines">
                        Criar Rotina
                    </button>
                </div>
            }
        </div>
    </div>
</div>

<div class="card">
    <div class="card-header">
        <h3 class="card-title">Biblioteca de Exercícios</h3>
        <button class="btn btn-sm btn-primary" @onclick="ManageExercises">
            + Novo Exercício
        </button>
    </div>
    <div class="card-body">
        @if (isLoadingExercises)
        {
            <div style="text-align: center; padding: 2rem;">
                <div class="loading"></div>
                <p style="margin-top: 1rem;">Carregando...</p>
            </div>
        }
        else if (exercises.Any())
        {
            <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); gap: 1rem;">
                @foreach (var exercise in exercises)
                {
                    <div style="padding: 1rem; background: var(--color-background-terciary); border-radius: var(--border-radius);">
                        <h4 style="margin: 0 0 0.5rem 0; font-size: 1rem;">@exercise.Name</h4>
                        <div style="display: flex; flex-direction: column; gap: 0.25rem; font-size: 0.875rem;">
                            <span class="badge badge-primary" style="width: fit-content;">@exercise.MuscleGroup</span>
                            <span style="color: var(--color-text-terciary);">@exercise.EquipmentType</span>
                        </div>
                    </div>
                }
            </div>
        }
        else
        {
            <div style="text-align: center; padding: 2rem;">
                <div style="font-size: 2.5rem; margin-bottom: 1rem;">??</div>
                <p style="color: var(--color-text-terciary);">Nenhum exercício cadastrado</p>
            </div>
        }
    </div>
</div>
</RedirectToLogin>

@code {
    [CascadingParameter] private Task<AuthenticationState> AuthenticationStateTask { get; set; } = default!;
    private int? _userId;

    private List<TrainingSessionDTO> recentWorkouts = new();
    private List<RoutineDTO> routines = new();
    private List<ExerciseDTO> exercises = new();
    private bool isLoadingWorkouts = true;
    private bool isLoadingRoutines = true;
    private bool isLoadingExercises = true;

    private int totalWorkouts => recentWorkouts.Count;
    private int weekWorkouts => recentWorkouts.Count(w => w.StartTime >= DateTime.Now.AddDays(-7));
    private int routinesCount => routines.Count;
    private int exercisesCount => exercises.Count;

    protected override async Task OnInitializedAsync()
    {
        await ResolveUserIdAsync();
        await Task.WhenAll(
            LoadWorkouts(),
            LoadRoutines(),
            LoadExercises()
        );
    }

    private async Task ResolveUserIdAsync()
    {
        var auth = await AuthenticationStateTask;
        var user = auth.User;
        var idStr = user.FindFirst(ClaimTypes.NameIdentifier)?.Value
                   ?? user.FindFirst("UserId")?.Value
                   ?? user.FindFirst("sub")?.Value;
        if (int.TryParse(idStr, out var id))
            _userId = id;
    }

    private async Task LoadWorkouts()
    {
        isLoadingWorkouts = true;
        try
        {
            var res = await GymService.GetTrainingSessionsAsync();
            if (res.Success && res.Data != null)
            {
                recentWorkouts = (_userId is null ? res.Data : res.Data.Where(x => x.UserId == _userId.Value)).OrderByDescending(w => w.StartTime).ToList();
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error loading workouts: {ex.Message}");
        }
        finally
        {
            isLoadingWorkouts = false;
        }
    }

    private async Task LoadRoutines()
    {
        isLoadingRoutines = true;
        try
        {
            var result = await GymService.GetRoutinesAsync();
            if (result.Success && result.Data != null)
            {
                routines = result.Data;
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error loading routines: {ex.Message}");
        }
        finally
        {
            isLoadingRoutines = false;
        }
    }

    private async Task LoadExercises()
    {
        isLoadingExercises = true;
        try
        {
            var result = await GymService.GetExercisesAsync();
            if (result.Success && result.Data != null)
            {
                exercises = result.Data;
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error loading exercises: {ex.Message}");
        }
        finally
        {
            isLoadingExercises = false;
        }
    }

    private async Task StartWorkout()
    {
        try
        {
            if (_userId is null || routines.Count == 0) return;
            var routineId = routines.First().Id; // simplificação
            var start = DateTime.Now;
            var end = DateTime.Now.AddMinutes(45);
            var cmd = new CreateTrainingSessionCommand(_userId.Value, routineId, start, end);
            await GymService.CreateTrainingSessionAsync(cmd);
            await LoadWorkouts();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error starting workout: {ex.Message}");
        }
    }

    private void StartRoutine(RoutineDTO routine)
    {
        // Abrir modal com detalhes/execução (a implementar)
    }

    private void ManageRoutines()
    {
        // Navegar ou abrir modal de CRUD (a implementar)
    }

    private void ManageExercises()
    {
        // Navegar ou abrir modal de CRUD (a implementar)
    }
}
