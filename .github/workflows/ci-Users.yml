name: CI - Users

on:
  push:
    paths:
      - "Services/Users/**"
  pull_request:
    paths:
      - "Services/Users/**"
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"
      - name: Restore solution
        run: dotnet restore LifeSync.slnx
      - name: Build solution
        run: dotnet build LifeSync.slnx --configuration Release --no-restore
      # - name: Run service tests (if any)
      #   run: |
      #     tests=$(find tests -type f \( -iname "*users*.csproj" -o -iname "*user*.csproj" -o -iname "*test*.csproj" -o -iname "*tests*.csproj" \))
      #     if [ -n "$tests" ]; then
      #       for t in $tests; do
      #         dotnet test "$t" --no-build --verbosity normal || exit $?
      #       done
      #     else
      #       echo "No tests found for Users"
      #     fi

  docker_build_and_push:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: read
      packages: write
    outputs:
      image_tag: ${{ steps.meta.outputs.tags }}
    steps:
      - uses: actions/checkout@v4

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/victormoraessantos/lifesync-usersapi
          tags: |
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Services/Users/Users.API/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  deploy_to_kubernetes:
    runs-on: ubuntu-latest
    needs: docker_build_and_push
    steps:
      - uses: actions/checkout@v4

      - name: Install kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: "latest"

      - name: Configure kubectl
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBE_CONFIG }}" | base64 -d > ~/.kube/config
          chmod 600 ~/.kube/config

      - name: Verify namespace access
        run: kubectl get deployments -n lifesync

      - name: Update image in deployment
        run: |
          kubectl set image deployment/users-api \
            users-api=ghcr.io/victormoraessantos/lifesync-usersapi:${{ github.sha }} \
            -n lifesync

      - name: Rollout status
        run: kubectl rollout status deployment/users-api -n lifesync --timeout=5m

      - name: Get deployment info
        run: kubectl get pods -n lifesync -l app=users-api -o wide
