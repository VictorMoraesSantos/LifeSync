name: CI/CD - TaskManager

on:
  push:
    branches: [main]
    paths:
      - "Services/TaskManager/**"
  pull_request:
    paths:
      - "Services/TaskManager/**"
  workflow_dispatch: {}

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore solution
        run: dotnet restore LifeSync.slnx

      - name: Build solution
        run: dotnet build LifeSync.slnx --configuration Release --no-restore

      - name: Run unit tests (TaskManager)
        run: dotnet test tests/TaskManager.UnitTests/TaskManager.UnitTests.csproj --no-build --configuration Release --verbosity normal

  docker_build_and_push:
    runs-on: ubuntu-latest
    needs: build_and_test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/victormoraessantos/lifesync-taskmanagerapi
          tags: |
            type=sha,prefix=main-

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Services/TaskManager/TaskManager.API/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  deploy:
    runs-on: ubuntu-latest
    needs: docker_build_and_push
    environment: production
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /opt/lifesync
            SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
            sed -i "s/^TASKMANAGER_TAG=.*/TASKMANAGER_TAG=main-${SHORT_SHA}/" .env
            docker compose -f docker-compose.apps.yml pull taskmanager.api
            docker compose -f docker-compose.apps.yml up -d taskmanager.api
